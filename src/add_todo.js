class TodoItem {
 constructor(title, description, dueDate, proirity, task) {
  this.title = title;
  this.description = description;
  this.dueDate = dueDate;
  this.proirity = proirity;
  this.task = task;
 }
}

export default TodoItem;